server:
  port: 8888
spring:
  application:
    name: learn-config
  cloud:
     config:
      server:
        git:
          uri: https://github.com/shanks-devil/config-repo.git
          basedir: /tmp/config-repo
          clone-on-start: true